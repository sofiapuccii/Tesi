# Configurazione per Clinical Analysis
# Calcola ST% da dati WEEK usando modello di classificazione

# Directory con modelli addestrati (best_model_classification.keras + metadata.json)
models_dir: "results/deep_learning_results_gravity/models"

# Directory base con dati preprocessati (dove cercare preprocessed/week/)
data_dir: "results/gravity_preprocessing"

# Path al file Excel con AHA scores clinici
metadata_path: "../dati_uniti/metadata2023_08.xlsx"

# Directory output per risultati (dove salvare regression_dataset.csv)
output_dir: "results/clinical_analysis_v2"